#Bulk File Extension Renamer 

#!/bin/bash
TARGET_DIR="${1:-.}"
EXT_OLD="log"
EXT_NEW="bak"

for file in "$TARGET_DIR"/*."$EXT_OLD"; do
    [ -f "$file" ] || continue
    mv  "$file" "${file%.$EXT_OLD}"."$EXT_NEW"
done

