#Script 2: Bulk File Extension Renamer (bulk_rename.sh)

#!/bin/bash
TARGET_DIR="${1:-.}"
EXT_OLD="log"
EXT_NEW="bak"

for file in "$TARGET_DIR"/*."$EXT_OLD"; do
    [ -f "$file" ] || continue
    base_name="${file%.$EXT_OLD}"
    mv "$file" "${base_name}.${EXT_NEW}"
    e